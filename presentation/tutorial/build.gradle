apply from: "$rootDir${BaseModules.FEATURE}"

dependencies {

    // Module dependencies
    implementation project(Modules.PRESENTATION_MAIN)

    // UI
    implementation Dependencies.PAGER_INDICATOR
}